<nav class="navbar navbar-expand-lg navbar-dark ftco_navbar bg-dark ftco-navbar-light" id="ftco-navbar">
    <div class="container">
        <a href='@Url.Action("Index", "Home")' class="imgtag">
            <img src='@Url.Content("~/Assets/images/Logo/logo.png")' />
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#ftco-nav"
                aria-controls="ftco-nav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="oi oi-menu"></span> Menu
        </button>
        <div class="collapse navbar-collapse" id="ftco-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item home-tag">@Html.ActionLink("Home", "Index", "Home", null, new { @class = "nav-link" })</li>
                <li class="nav-item home-tag">@Html.ActionLink("Shop", "Index", "Shop", new { id = Session["MemberID"] }, new { @class = "nav-link" })</li>
                <li class="nav-item home-tag">@Html.ActionLink("About", "About", "Home", null, new { @class = "nav-link" })</li>
                @if (User.Identity.IsAuthenticated)
                //是否通過驗證 //User.Identity.IsAuthenticated、Request.IsAuthenticated
                {                                                        //User.Identity.Name 可擷取目前登入的使用者帳戶資訊
                    <li class="nav-item">@Html.ActionLink("HiHi!! " + ((DotrA.Service.Security.Identity)User.Identity).MemberName, "Index", "Home", null, htmlAttributes: new { @class = "nav-link" })</li>
                    <li id="Cart" class="nav-item cta cta-colored dropdown">@Html.Partial("_CartPartial")</li>
                    <li class="nav-item">@Html.ActionLink("Logout", "Logout", "Member", null, new { @class = "nav-link" })</li>
                }
                else
                {
                    <li class="nav-item dropdown ">
                        <a class="nav-link dropdown-toggle" id="dropdown04" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-users"></i>Register/Login
                        </a>
                        <div class="dropdown-menu " aria-labelledby="dropdown04">
                            <a class="dropdown-item " href="@Url.Action("Register","Member")">Register</a>
                            @*<a class="dropdown-item " href="@Url.Action("Login","Members")">Login</a>*@
                            <a class="dropdown-item " data-toggle="modal" data-target="#LoginModal">Login</a>
                        </div>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>
@if (!User.Identity.IsAuthenticated)
{
    <!-- Modal -->
    <div class="modal" id="LoginModal" tabindex="-1" role="dialog">
        @{Html.RenderPartial("_LoginModal", new DotrA.Models.Member.LoginViewModel());}
    </div>
}
